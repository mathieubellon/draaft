tasks:
    - type save config frontmapper @done
    - read/parse frontmapper @done
    - order cargo
    - apiToken in $USER_HOME ? (so we can version config file for types)
    - Cut top directory (pull option)
    - Body is not in fm by default and type file can override with "true".
    - Token API pas avec underscore ou tiret
